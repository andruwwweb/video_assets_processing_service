# Dev: proxy target for /v1 and /openapi.json rewrites (the API).
API_PROXY_TARGET=http://localhost:3000
# WebSocket base for real-time task progress (connects directly to the API).
NEXT_PUBLIC_WS_URL=ws://localhost:3000
